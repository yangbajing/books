# 使用 Rust 构建 gRPC 微服务

```sh
# 本地预览
mdbook serve --open

# 发布
mdbook build
```

> - 安装 mdbook 需要安装 rust 和 cargo，可以使用 [rustup](https://rustup.rs/) 安装。
> - mdbook 是一个基于 Rust 的 Markdown 文档生成器，可以用来生成静态网站。安装方式： `cargo install mdbook`。
